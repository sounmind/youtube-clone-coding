extends layouts/main
include mixins/videoBlock

block content
    .videos <!-- 'h1=' 처럼 붙여 써줘야 함! -->
        each video in videos
            +videoBlock({
                title : video.title,
                views : video.description,
                videoFile: video.videoFile
            })